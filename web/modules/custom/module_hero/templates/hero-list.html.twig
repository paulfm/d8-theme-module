{{ attach_library('module_hero/hero-style') }}
<h4 class="animated bounce">{{ title }}</h4>
<ol>
    {% for item in items %}
        <li class="animated {{ random(['bounce', 'flash', 'rubberBand', 'shake', 'jello', 'tada', 'bounceIn', 'rollIn']) }}">{{ item.name }}</li>
    {% endfor %}
</ol>
<strong>This is from our new TWIG file</strong>